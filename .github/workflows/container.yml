name: Docker
on: 
  push:
  pull_request:
jobs:
  docker-container:
    runs-on: ubuntu-latest
    steps:
      - name: print os release
        run: cat /etc/os-release
    container:
      image: ubuntu:18.04
      


  


      